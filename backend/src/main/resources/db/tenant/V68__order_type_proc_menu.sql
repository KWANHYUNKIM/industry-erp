-- 오더관리유형의 [처리메뉴] — 원본 오더관리유형등록(ESO001M)의 조건(코드도움 txtProcMenu).
--
-- 이 유형을 <b>어느 입력 화면에서 고를 수 있나</b>. 우리는 [입력메뉴에서 사용]이라는
-- 예/아니오만 있었다 — 쓴다는 것은 알아도 <b>어디서</b> 쓰는지는 알 수 없었다.
-- 유형이 늘어나면 주문서 화면의 유형 목록에 발주용·A/S용까지 다 뜨게 된다.
--
-- 화면 경로를 글자로 담는다(예: /sales/orders). 메뉴는 코드가 아니라 화면이라
-- 참조할 테이블이 없다.
ALTER TABLE order_types ADD COLUMN proc_menu varchar(200);
