-- 휴가 사용일수를 시간 단위까지 담는다.
--
-- numeric(5,1) 이라 DB 가 0.125(1시간)를 0.1 로 잘라 버렸다. 엔티티도 scale=1 이었다.
-- 원본(이카운트) 휴가사용실적현황에는 0.13 · 0.25 같은 값이 그대로 남아 있다.
-- 반차(0.5)만 생각하고 잡은 자릿수인데, 시간 단위 휴가를 쓰는 순간 잔여일수가 어긋난다.
ALTER TABLE vacation_requests ALTER COLUMN days TYPE numeric(6,3);
