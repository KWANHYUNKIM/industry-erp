-- 거래처·품목의 [검색창내용].
--
-- 원본 거래처검색 팝업(거래처관리대장 II 사본)의 조건에도, 거래처리스트의 열에도,
-- 품목등록 리스트에도 **검색창내용** 이 있다. 공식 이름 말고 사람들이 실제로 부르는
-- 이름(약칭·영문명·옛 상호·별명)을 적어 두고 그걸로 찾는 칸이다.
--
-- 우리 코드도움은 코드와 이름 두 가지로만 찾는다. 그래서 '한국기계전기전자시험연구원' 을
-- 'KTC' 로 기억하는 사람은 목록을 눈으로 훑는 수밖에 없었다. 거래처 300곳이 넘으면
-- 그게 코드도움을 쓰는 이유를 지운다.
--
-- nullable 이다. 기존 행에 백필할 값이 없다 — 사람이 아는 별명을 지어낼 수는 없다.

ALTER TABLE business_partners ADD COLUMN search_keyword varchar(200);
ALTER TABLE items ADD COLUMN search_keyword varchar(200);
