-- 출하 라인의 [적요].
--
-- 원본 출하지시서입력 그리드의 마지막 열이 적요다(품목 · 품목명 · 규격 · 수량 · 적요).
-- 출하지시서현황·출하현황의 결과 열에도 적요가 들어간다.
--
-- 우리 출하는 전표 단위 적요만 있어서 "이 품목만 왜 따로 보내는지" 를 줄에 적을 자리가
-- 없었다. 판매·구매·생산불출 라인은 이미 다 줄 적요를 들고 있다.
alter table shipment_lines add column remark varchar(255);
